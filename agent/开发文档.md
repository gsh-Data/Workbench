import os
import markdown
from weasyprint import HTML

md_content = """# 个人专属全栈工作台 V3.0 架构与开发方案 (无数据库 + 类App一键启动版)

## 一、 项目概览
本项目旨在打造一个专属于个人的全栈工作台，作为日常开发、知识沉淀与效率管理的“超级控制中心”。系统采用 Web 架构，原生支持 PC 与手机端自适应访问。

**🚀 V3.0 核心亮点：**
1. **完全摒弃传统数据库**，采用纯文件系统（JSON + MD）进行数据持久化，实现极致的轻量化。
2. **“类 App” 沉浸式体验**，配合一键启动脚本，双击图标即可静默启动前后端并弹出独立工作台窗口。

*   **核心技术栈：** Vue 3 (前端) + Node.js (后端)
*   **开发流：** 结合 Antigravity 2.0 进行细粒度组件拆分与 AI 辅助生成

---

## 二、 纯文件存储架构设计 (核心亮点)

抛弃 SQLite 等一切关系型/非关系型数据库软件，业务数据管理回归最原始高效的文件读写。

### 1. 存储结构设计 (`data/db.json`)
在 Node.js 后端项目根目录下建立一个 `data` 文件夹，所有结构化数据集中在 JSON 文件中管理（可按业务分多个 JSON，如 `snippets.json`, `cron.json`）：
```json
{
  "snippets": [
    { "id": "1", "title": "Vue3 生命周期模板", "code": "...", "tags": ["vue", "setup"] }
  ],
  "cronJobs": [
    { "id": "job1", "name": "每日系统清理", "schedule": "0 0 * * *", "status": "active" }
  ],
  "todos": []
}